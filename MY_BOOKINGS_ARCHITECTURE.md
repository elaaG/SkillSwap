# 🏗️ My Bookings - Architecture & Flow

## 📐 Architecture Globale

```
┌─────────────────────────────────────────────────────────────┐
│                    BLAZOR WEBASSEMBLY APP                   │
│                   (SkillSwap.Client)                        │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ HTTPS
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    ASP.NET CORE API                         │
│                   (SkillSwap.Api)                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ EF Core
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    SQLite DATABASE                          │
│                   (SkillSwap.db)                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 Component Hierarchy

```
MyBookings.razor (Page)
├── @page "/my-bookings"
├── @attribute [Authorize]
│
├── States:
│   ├── Loading → Spinner
│   ├── Error → Error Message + Retry Button
│   ├── Empty → Empty State + Browse Button
│   └── Success → Stats Bar + Bookings Grid
│
└── BookingsGrid (when Success)
    │
    ├── BookingCard #1
    │   ├── BookingStatusBadge
    │   ├── EscrowStatusBadge
    │   └── Action Buttons (conditional)
    │
    ├── BookingCard #2
    │   ├── BookingStatusBadge
    │   ├── EscrowStatusBadge
    │   └── Action Buttons (conditional)
    │
    └── BookingCard #N
        ├── BookingStatusBadge
        ├── EscrowStatusBadge
        └── Action Buttons (conditional)
```

---

## 🔄 Data Flow Diagram

### 1. Initial Load

```
┌──────────────┐
│   Browser    │
│  /my-bookings│
└──────┬───────┘
       │ Navigate
       ▼
┌──────────────────┐
│ MyBookings.razor │
│ OnInitialized()  │
└──────┬───────────┘
       │ Call
       ▼
┌──────────────────────┐
│  IBookingService     │
│  GetMyBookingsAsync()│
└──────┬───────────────┘
       │ HttpClient.Get
       ▼
┌──────────────────────────┐
│  HttpClient + AuthHeader │
│  GET /api/bookings/my    │
└──────┬───────────────────┘
       │ HTTP Request
       ▼
┌──────────────────────┐
│  BookingController   │
│  GetMyBookings()     │
└──────┬───────────────┘
       │ Query
       ▼
┌──────────────────────┐
│  BookingService      │
│  GetMyBookingsAsync()│
└──────┬───────────────┘
       │ EF Core
       ▼
┌──────────────────────┐
│  ApplicationDbContext│
│  Bookings.Where()    │
└──────┬───────────────┘
       │ SQL Query
       ▼
┌──────────────────────┐
│  SQLite Database     │
│  SELECT * FROM...    │
└──────┬───────────────┘
       │ Results
       ▼
┌──────────────────────┐
│  List<BookingDto>    │
│  Return to Client    │
└──────┬───────────────┘
       │ JSON Response
       ▼
┌──────────────────────┐
│  MyBookings.razor    │
│  Bookings = result   │
│  StateHasChanged()   │
└──────┬───────────────┘
       │ Render
       ▼
┌──────────────────────┐
│  UI Updates          │
│  Grid with Cards     │
└──────────────────────┘
```

### 2. Accept Booking Flow

```
┌──────────────┐
│    User      │
│  Click Accept│
└──────┬───────┘
       │
       ▼
┌──────────────────┐
│ BookingCard.razor│
│ OnAcceptAsync()  │
│ IsProcessing=true│
└──────┬───────────┘
       │
       ▼
┌──────────────────────┐
│  IBookingService     │
│  AcceptBookingAsync()│
└──────┬───────────────┘
       │
       ▼
┌────────────────────────────┐
│  HttpClient                │
│  POST /api/bookings/{id}/  │
│       accept               │
└──────┬─────────────────────┘
       │
       ▼
┌──────────────────────┐
│  BookingController   │
│  Accept(id)          │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│  BookingService      │
│  AcceptAsync(id)     │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│  Database            │
│  UPDATE State        │
│  = Accepted          │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│  200 OK Response     │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ BookingCard.razor    │
│ Booking.State        │
│ = "Accepted"         │
│ IsProcessing=false   │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│  UI Updates          │
│  Badge → ACCEPTED    │
│  Show Complete Btn   │
└──────────────────────┘
```

### 3. Complete Booking Flow (avec transfert d'argent)

```
┌──────────────┐
│    User      │
│ Click Complete│
└──────┬───────┘
       │
       ▼
┌──────────────────┐
│ BookingCard.razor│
│ OnCompleteAsync()│
└──────┬───────────┘
       │
       ▼
┌──────────────────────┐
│  IBookingService     │
│ CompleteBookingAsync()│
└──────┬───────────────┘
       │
       ▼
┌────────────────────────────┐
│  BookingController         │
│  Complete(id)              │
└──────┬─────────────────────┘
       │
       ▼
┌────────────────────────────┐
│  BookingService            │
│  CompleteAsync(id)         │
│  BEGIN TRANSACTION         │
└──────┬─────────────────────┘
       │
       ├─► UPDATE Booking.State = Completed
       │
       ├─► UPDATE Escrow.Status = Released
       │
       ├─► UPDATE ClientWallet
       │   ├─ EscrowBalance -= Amount
       │   └─ (déjà débité)
       │
       ├─► UPDATE ProviderWallet
       │   └─ AvailableBalance += Amount
       │
       ├─► INSERT TransactionLog
       │   ├─ FromUserId = ClientId
       │   ├─ ToUserId = ProviderId
       │   ├─ Amount = Escrow.Amount
       │   └─ Type = BookingPayment
       │
       └─► COMMIT TRANSACTION
           │
           ▼
       ┌──────────────────────┐
       │  200 OK Response     │
       └──────┬───────────────┘
              │
              ▼
       ┌──────────────────────┐
       │ BookingCard.razor    │
       │ State = "Completed"  │
       │ Escrow = "Released"  │
       └──────┬───────────────┘
              │
              ▼
       ┌──────────────────────┐
       │  UI Updates          │
       │  Badge → COMPLETED   │
       │  Badge → ✅ Released │
       │  No buttons          │
       └──────────────────────┘
```

---

## 🎨 State Machine - Booking States

```
┌─────────────────────────────────────────────────────────┐
│                   BOOKING LIFECYCLE                     │
└─────────────────────────────────────────────────────────┘

    CREATE
      │
      ▼
┌───────────┐
│  PENDING  │ ◄─────────┐
│ 🔒 Hold   │           │
└─────┬─────┘           │
      │                 │
      │ [Accept]        │ [Reject]
      │                 │
      ▼                 │
┌───────────┐           │
│ ACCEPTED  │           │
│ 🔒 Hold   │           │
└─────┬─────┘           │
      │                 │
      │ [Complete]      │
      │                 │
      ▼                 ▼
┌───────────┐     ┌───────────┐
│ COMPLETED │     │ REJECTED  │
│✅ Released│     │↩️ Refunded│
└───────────┘     └───────────┘
  (FINAL)           (FINAL)
```

---

## 🔐 Authentication Flow

```
┌──────────────┐
│    User      │
│  Login       │
└──────┬───────┘
       │
       ▼
┌──────────────────┐
│  AuthService     │
│  LoginAsync()    │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  API /auth/login │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  JWT Token       │
│  Returned        │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  LocalStorage    │
│  Save Token      │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ AuthHeaderHandler│
│ Intercepts all   │
│ HTTP requests    │
└──────┬───────────┘
       │
       ▼
┌──────────────────────────┐
│  All API Calls           │
│  Header: Authorization:  │
│  Bearer {token}          │
└──────────────────────────┘
```

---

## 💾 Database Schema (Relevant Tables)

```sql
┌─────────────────────────────────────────┐
│              Bookings                   │
├─────────────────────────────────────────┤
│ Id (PK)               GUID              │
│ ClientId (FK)         STRING            │
│ ProviderId (FK)       STRING            │
│ ListingId (FK)        GUID              │
│ StartTime             DATETIME          │
│ EndTime               DATETIME          │
│ State                 INT (Enum)        │
│   0 = Pending                           │
│   1 = Accepted                          │
│   2 = Completed                         │
│   3 = Rejected                          │
└─────────────────────────────────────────┘
           │
           │ 1:1
           ▼
┌─────────────────────────────────────────┐
│         EscrowTransactions              │
├─────────────────────────────────────────┤
│ Id (PK)               INT               │
│ BookingId (FK)        GUID              │
│ Amount                DECIMAL(18,2)     │
│ Status                INT (Enum)        │
│   0 = Hold                              │
│   1 = Released                          │
│   2 = Refunded                          │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│              Wallets                    │
├─────────────────────────────────────────┤
│ Id (PK)               INT               │
│ UserId (FK)           STRING            │
│ AvailableBalance      DECIMAL(18,2)     │
│ EscrowBalance         DECIMAL(18,2)     │
│ CreatedAt             DATETIME          │
│ UpdatedAt             DATETIME          │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│         TransactionLogs                 │
├─────────────────────────────────────────┤
│ Id (PK)               INT               │
│ FromUserId (FK)       STRING (nullable) │
│ ToUserId (FK)         STRING            │
│ Amount                DECIMAL(18,2)     │
│ Type                  INT (Enum)        │
│ Timestamp             DATETIME          │
│ Notes                 STRING            │
│ TransactionReference  STRING            │
└─────────────────────────────────────────┘
```

---

## 🎨 CSS Architecture

```
┌─────────────────────────────────────────┐
│        Global Styles (App.css)          │
│  - CSS Variables                        │
│  - Reset styles                         │
│  - Typography base                      │
└─────────────────────────────────────────┘
                  │
                  │ Inherited by
                  ▼
┌─────────────────────────────────────────┐
│     Component-Scoped Styles             │
│                                         │
│  MyBookings.razor                       │
│    <style>                              │
│      .my-bookings-page { ... }          │
│      .bookings-grid { ... }             │
│      .stats-bar { ... }                 │
│    </style>                             │
│                                         │
│  BookingCard.razor                      │
│    <style>                              │
│      .booking-card { ... }              │
│      .btn-accept { ... }                │
│    </style>                             │
│                                         │
│  BookingStatusBadge.razor               │
│    <style>                              │
│      .booking-badge { ... }             │
│      .badge-pending { ... }             │
│    </style>                             │
└─────────────────────────────────────────┘
```

---

## 🧩 Dependency Injection Chain

```
Program.cs
  │
  ├─► builder.Services.AddScoped<IAuthService, AuthService>()
  ├─► builder.Services.AddScoped<IWalletService, WalletService>()
  ├─► builder.Services.AddScoped<IBookingService, BookingService>() ✨ NEW
  └─► builder.Services.AddScoped<HttpClient>()
        │
        └─► Configured with AuthHeaderHandler
              │
              └─► Adds "Authorization: Bearer {token}" to all requests
```

**Usage dans un composant** :
```razor
@inject IBookingService BookingService

@code {
    // Automatically injected, ready to use!
    await BookingService.GetMyBookingsAsync();
}
```

---

## 📦 Package Dependencies

```
SkillSwap.Client.csproj
├── Microsoft.AspNetCore.Components.WebAssembly (8.0)
├── Microsoft.AspNetCore.Components.Authorization (8.0)
├── Blazored.LocalStorage (4.x)
└── System.Net.Http.Json (8.0)
```

**Aucun nouveau package requis** ✅

---

## 🔄 Responsive Grid Breakpoints

```css
/* Desktop (> 768px) */
.bookings-grid {
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
}
/* Result: 3-4 columns */

/* Tablet (≤ 768px) */
@media (max-width: 768px) {
    .bookings-grid {
        grid-template-columns: 1fr;
    }
}
/* Result: 1 column */

/* Mobile (≤ 480px) */
@media (max-width: 480px) {
    .stats-bar {
        grid-template-columns: 1fr;
    }
}
/* Result: Stats in 1 column */
```

---

## ⚡ Performance Optimizations

### 1. Lazy Loading Components
```razor
@* Future improvement *@
<Virtualize Items="@bookings" Context="booking">
    <BookingCard Booking="@booking" />
</Virtualize>
```

### 2. AsNoTracking for Read-Only Queries
```csharp
// Backend - BookingService.cs
var bookings = await _context.Bookings
    .AsNoTracking()  // No change tracking = faster
    .ToListAsync();
```

### 3. Conditional Rendering
```razor
@if (IsLoading)
{
    <LoadingSpinner />
}
else if (HasError)
{
    <ErrorMessage />
}
else
{
    <BookingsGrid />
}
```

---

## 🎯 Summary

Cette architecture suit les **best practices** :
- ✅ Separation of Concerns
- ✅ Dependency Injection
- ✅ Repository Pattern (via EF Core)
- ✅ Component-based UI
- ✅ Responsive Design
- ✅ Error Handling at all levels
- ✅ SOLID Principles

**Result** : Code maintenable, testable, et évolutif ! 🚀
